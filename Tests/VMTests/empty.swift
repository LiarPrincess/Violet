import XCTest
@testable import VM

// For now we don't have any tests for 'VM'
// (mostly because we moved everything to 'Objects').
class EmptyTests: XCTestCase {

  func test_dummy() {
    XCTAssert(true)
  }
}
